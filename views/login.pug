doctype html
html(lang='en')
  head
    meta(charset='UTF-8')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')
    title #{title}
    style.
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        padding: 1rem;
        position: relative;
        overflow: hidden;
      }
      body::before {
        content: '🌸';
        position: absolute;
        font-size: 3rem;
        animation: float1 15s infinite ease-in-out;
        top: 10%;
        left: 10%;
        opacity: 0.6;
      }
      body::after {
        content: '🌺';
        position: absolute;
        font-size: 2.5rem;
        animation: float2 12s infinite ease-in-out;
        top: 70%;
        right: 15%;
        opacity: 0.6;
      }
      .flower {
        position: absolute;
        font-size: 2rem;
        opacity: 0.5;
        pointer-events: none;
        z-index: 1;
      }
      .flower1 { top: 20%; left: 80%; animation: float3 18s infinite; }
      .flower2 { top: 60%; left: 5%; animation: float1 20s infinite; }
      .flower3 { top: 15%; right: 20%; animation: float2 16s infinite; }
      .flower4 { bottom: 20%; left: 15%; animation: float3 14s infinite; }
      .flower5 { top: 80%; right: 10%; animation: float1 17s infinite; }
      .flower6 { top: 40%; left: 8%; animation: float2 19s infinite; }
      .flower7 { bottom: 10%; right: 25%; animation: float3 15s infinite; }
      .flower8 { top: 50%; right: 5%; animation: float1 13s infinite; }
      @keyframes float1 {
        0%, 100% { transform: translateY(0) rotate(0deg); }
        25% { transform: translateY(-20px) rotate(5deg); }
        50% { transform: translateY(-10px) rotate(-5deg); }
        75% { transform: translateY(-25px) rotate(3deg); }
      }
      @keyframes float2 {
        0%, 100% { transform: translateX(0) rotate(0deg); }
        25% { transform: translateX(15px) rotate(-5deg); }
        50% { transform: translateX(-10px) rotate(5deg); }
        75% { transform: translateX(20px) rotate(-3deg); }
      }
      @keyframes float3 {
        0%, 100% { transform: translate(0, 0) rotate(0deg); }
        33% { transform: translate(10px, -15px) rotate(5deg); }
        66% { transform: translate(-15px, -10px) rotate(-5deg); }
      }
      .login-container {
        background: white;
        padding: 2.5rem;
        border-radius: 20px;
        box-shadow: 0 20px 60px rgba(0,0,0,0.3);
        width: 100%;
        max-width: 420px;
        position: relative;
        overflow: hidden;
        z-index: 10;
      }
      .login-container::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 5px;
        background: linear-gradient(90deg, #ff6b6b, #ffd93d, #6bcf7f, #4d96ff, #ff6b6b);
        background-size: 200% 100%;
        animation: gradient 3s linear infinite;
      }
      @keyframes gradient {
        0% { background-position: 0% 0%; }
        100% { background-position: 200% 0%; }
      }
      .header {
        text-align: center;
        margin-bottom: 2rem;
        position: relative;
      }
      .header::before {
        content: '🌷';
        position: absolute;
        left: 10%;
        top: 0;
        font-size: 1.5rem;
        animation: rotate 4s infinite;
      }
      .header::after {
        content: '🌹';
        position: absolute;
        right: 10%;
        top: 0;
        font-size: 1.5rem;
        animation: rotate 4s infinite reverse;
      }
      @keyframes rotate {
        0%, 100% { transform: rotate(0deg); }
        50% { transform: rotate(15deg); }
      }
      h1 {
        color: #2d3748;
        font-size: 2rem;
        margin-bottom: 0.5rem;
        font-weight: 600;
      }
      .subtitle {
        color: #718096;
        font-size: 0.9rem;
      }
      .form-group {
        margin-bottom: 1.5rem;
        position: relative;
      }
      label {
        display: block;
        margin-bottom: 0.5rem;
        color: #4a5568;
        font-weight: 500;
        font-size: 0.95rem;
      }
      label::before {
        content: '🌼';
        margin-right: 0.5rem;
        font-size: 0.9rem;
      }
      input[type="text"],
      input[type="password"] {
        width: 100%;
        padding: 0.9rem;
        border: 2px solid #e2e8f0;
        border-radius: 10px;
        font-size: 1rem;
        transition: all 0.3s ease;
        background-color: #f7fafc;
      }
      input[type="text"]:focus,
      input[type="password"]:focus {
        outline: none;
        border-color: #667eea;
        background-color: white;
        box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
      }
      input::placeholder {
        color: #a0aec0;
        font-size: 0.9rem;
      }
      button {
        width: 100%;
        padding: 1rem;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        border: none;
        border-radius: 10px;
        cursor: pointer;
        font-size: 1.05rem;
        font-weight: 600;
        transition: all 0.3s ease;
        margin-top: 1rem;
        position: relative;
      }
      button::before {
        content: '🎉';
        margin-right: 0.5rem;
      }
      button::after {
        content: '🎉';
        margin-left: 0.5rem;
      }
      button:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
      }
      button:active {
        transform: translateY(0);
      }
      .error {
        color: #f56565;
        background-color: #fff5f5;
        padding: 0.75rem;
        border-radius: 8px;
        text-align: center;
        margin-bottom: 1.5rem;
        border-left: 4px solid #f56565;
        font-size: 0.9rem;
      }
      .hint-text {
        color: #a0aec0;
        font-size: 0.85rem;
        margin-top: 0.3rem;
        font-style: italic;
      }
      .hint-text::before {
        content: '💡 ';
      }
      @media (max-width: 480px) {
        .login-container {
          padding: 2rem;
        }
        h1 {
          font-size: 1.75rem;
        }
      }
  body
    .flower.flower1 🌸
    .flower.flower2 🌺
    .flower.flower3 🌻
    .flower.flower4 🌷
    .flower.flower5 🌹
    .flower.flower6 🏵️
    .flower.flower7 💐
    .flower.flower8 🌼
    .login-container
      .header
        h1 Magic Gate
        .subtitle Celebrate with us on 20/10
      if error
        .error= error
      form(action="/login" method="POST")
        .form-group
          label(for="username") Username
          input(type="text" id="username" name="username" placeholder="username" required)
          .hint-text Hint: last + first
        .form-group
          label(for="password") Password
          input(type="password" id="password" name="password" placeholder="password" required)
          .hint-text Hint: ani
        button(type="submit") Login